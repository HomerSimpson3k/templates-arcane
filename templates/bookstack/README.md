# BookStack

Set `APP_URL` to the public URL users will use to access BookStack before first setup.

`APP_KEY` must stay stable after installation. The sample value is only for first-run testing; replace it with a generated Laravel-style key before storing real data.
