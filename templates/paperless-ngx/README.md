# Paperless-ngx

Paperless-ngx stores documents in Docker volumes and watches the local `consume` folder for imports.

Change `PAPERLESS_SECRET_KEY` before using this with real documents. Set `PAPERLESS_URL` to the externally reachable URL when running behind a reverse proxy.
