# Grafana and Prometheus

Grafana is pre-provisioned with Prometheus as a datasource. Sign in with `GRAFANA_ADMIN_USER` and `GRAFANA_ADMIN_PASSWORD`, then add dashboards for Prometheus and Node Exporter.

This stack includes Node Exporter for host metrics and mounts host paths read-only.
