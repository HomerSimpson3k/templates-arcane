# n8n

Change all database passwords, `N8N_ENCRYPTION_KEY`, and `RUNNERS_AUTH_TOKEN` before using this template for real workflows.

Set `N8N_HOST`, `N8N_PROTOCOL`, and `WEBHOOK_URL` to the externally reachable URL if webhooks or OAuth callbacks will be used.
