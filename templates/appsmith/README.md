# Appsmith

Replace `APPSMITH_ENCRYPTION_PASSWORD` and `APPSMITH_ENCRYPTION_SALT` before storing real data. These values protect encrypted datasource credentials and should remain stable after setup.
