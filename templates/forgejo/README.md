# Forgejo

This template starts Forgejo with its built-in SQLite option for a lightweight first setup. Larger installations should move to PostgreSQL or MySQL before heavy use.

Set `FORGEJO_ROOT_URL`, `FORGEJO_DOMAIN`, and `FORGEJO_SSH_DOMAIN` to the real hostname before inviting users.
