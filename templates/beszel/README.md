# Beszel

This template starts the Beszel hub and a local agent on the same host.

After the hub starts, add the local system in the Beszel UI, copy the generated `KEY` and `TOKEN` values into `.env`, and restart the stack so the agent can connect.
